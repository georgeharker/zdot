# zdot_exclude — zdot infrastructure files not installed as dotfiles.
# Patterns are relative to the zdot repo root.
.shuck.toml

# Documentation only — everything else in zdot is installed. Anchored
# to the repo root like /tests/.
/docs/

# Test suite — repo infrastructure, never linked into ~/.config/zdot
/tests/

# CI / GitHub metadata
.github/
