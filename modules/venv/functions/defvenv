#!/usr/bin/env zsh
# Activate virtual environment

defvenv() {
    local venv=$HOME/.venv
    if [[ -d .venv ]] ; then
        source ${venv}/bin/activate
    else
        echo "No venv (${venv}) found"
        return 1
    fi
}
